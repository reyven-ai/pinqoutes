export type Comments = {
  userId: string;
  pinId: string;
  comment: string;
  created_at: Date;
};

export type CommentsData = {
  userId: string;
  pinId: string;
  comment: string;
  created_at: Date;
};
