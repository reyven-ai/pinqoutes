version: "3.9"

services:
  postgres:
    image: postgres:16
    ports:
      - 5432:5432
    volumes:
      - ~/apps/clone-pinterest/postgres-db:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: pinterest
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Villaester03-
